{
  "name": "discord-teams",
  "version": "1.0.0",
  "main": "dist/index.js",
  "source": "src/index.ts",
  "scripts": {
    "start": "parcel watch src/index.ts",
    "prebuild": "rimraf dist",
    "build": "parcel build src/index.ts --no-cache --no-source-maps",
    "clean": "rimraf .parcel-cache dist",
    "clean:modules": "yarn clean; node_modules"
  },
  "license": "MIT",
  "dependencies": {
    "discord.js": "^12.2.0"
  },
  "devDependencies": {
    "parcel": "^2.0.0-beta.1",
    "rimraf": "^3.0.2"
  }
}
